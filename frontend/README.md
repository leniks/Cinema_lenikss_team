0. исправьте под свой комп пути (у меня где-то не работает относительный)

1. запуск фронта
npm install
npm run dev

2. запуск бэка

uvicorn app.main:app --reload --port 8000

3. http://localhost:3000 открывайте

!!!!! если будет писать, что какой-то адрес занят:
lsof -i :8000 (или lsof -i :3000)
kill -9 39129 (вместо последнего числа PID)