docker-compose up -d (развертывание бд из корня)

alembic revision --autogenerate -m "Migration" (создание миграции)

alembic upgrade head (применение миграции)

uvicorn app.main:app --reload (запуск сервиса из корня) 
